MCI Management System (Enterprise Cloud Edition)ระบบบริหารจัดการอุบัติภัยหมู่ระดับชาติ (National Standard)แพลตฟอร์ม Web Application ที่ถูกออกแบบมาสำหรับ ผู้บัญชาการเหตุการณ์ (MCI Commander) และ ทีมแพทย์/กู้ชีพ (Paramedics & Technical Rescuers) เพื่อรับมือกับอุบัติภัยหมู่ (Mass Casualty Incident) อย่างเป็นระบบ รวดเร็ว และมีประสิทธิภาพระบบได้รับการอัปเกรดเป็น Serverless Architecture รองรับการทำงานแบบ Offline-first และซิงค์ข้อมูลขึ้น Cloud Real-time พร้อมกันนับร้อยเครื่อง (Concurrency)🌟 ฟีเจอร์เด่น (Core Features)Real-time Cloud Sync + Offline Mode:บันทึกข้อมูลลงเครื่องมือถือทันทีแม้อยู่ในจุดอับสัญญาณซิงค์ขึ้นระบบส่วนกลางอัตโนมัติเมื่อสัญญาณอินเทอร์เน็ตกลับมา (แสดงสถานะ Sync ที่มุมจอ)Hospital ISOS (ระบบศักยภาพโรงพยาบาล):ตรวจสอบจำนวนเตียงว่างของ รพ. ในเครือข่ายแบบ Real-timeเมื่อทำการนำส่งผู้ป่วย (Dispatch) ระบบจะหักลบจำนวนเตียงที่รองรับได้อัตโนมัติ ป้องกัน รพ. ปลายทางล้นAdvanced Triage & Tech Rescue:รองรับ TST (Ten Second Triage) สำหรับ Primary Triage และ MITT (NHS Major Incident Triage Tool) สำหรับ Secondary Triageรองรับการสแกน Barcode/QR จาก SMART Tag และ METTAGมีฟิลด์เฉพาะกิจสำหรับ HazMat (การล้างพิษ) และ Technical Rescue (เวลาตัดถ่าง/Extrication Time)Media & Photo Documentation:ถ่ายภาพบาดแผลหรือกลไกการบาดเจ็บ แนบไปกับ Patient ID ได้ทันที (ภาพถูกบีบอัดอัตโนมัติเพื่อประหยัดเน็ต)AI-Powered Command (Gemini API):วิเคราะห์ METHANE Report และเสนอแผนปฏิบัติการฉุกเฉิน 3 ข้อแรกวิเคราะห์ Vitals / GCS เพื่อประเมินระดับ Triage อัตโนมัติสร้างรายงานสรุป AAR (After Action Review) อัตโนมัติเมื่อจบภารกิจMass Notification Alert:แจ้งเตือนฉุกเฉินไปยังเครือข่ายทันทีเมื่อประกาศแผน "MCI Declared"🛠️ โครงสร้างเทคโนโลยี (Tech Stack)Frontend: HTML5, JavaScript (ES6+), Tailwind CSSMapping: Leaflet.js (Offline-friendly)Database & Auth: Google Cloud Firebase / Firestore (Real-time NoSQL)Backend (Optional Hosting): Google Apps Script (GAS) สำหรับการรันแบบ Web App ภายในองค์กร🚀 การติดตั้ง (Deployment Guide)1. แบบ Serverless (Firebase - แนะนำสำหรับการทำงานพร้อมกันหลายคน)ระบบนี้ทำงานเชื่อมต่อกับฐานข้อมูลแบบ Serverless ผ่าน API โดยตรงสร้างโปรเจกต์ใหม่ที่ Firebase Console (ตั้งค่า Authentication แบบ Anonymous หรือ Custom Token และสร้าง Firestore Database)นำ Firebase Config ที่ได้มาใส่ในตัวแปร Environment ของเว็บโฮสติ้ง หรือรันผ่าน Canvas นี้ได้เลย2. แบบใช้ Google Apps Script (สำหรับองค์กรที่ต้องการ Backup ลง Google Sheets)สร้าง Google Sheet ใหม่ (ตั้งชื่อ เช่น MCI_Database)ไปที่เมนู ส่วนขยาย (Extensions) -> Apps Scriptลบโค้ดเริ่มต้นทิ้ง และคัดลอกโค้ดจากไฟล์ Code.gs วางลงไปสร้างไฟล์ HTML ใหม่ในเมนู Apps Script โดยตั้งชื่อว่า Index และคัดลอกโค้ดจากไฟล์ index.html ไปวางใน Apps Script เลือกฟังก์ชัน setupDatabase แล้วกด "เรียกใช้ (Run)" (กดอนุญาตสิทธิ์) เพื่อให้ระบบสร้างตารางกด "การทำให้ใช้งานได้ (Deploy)" -> เลือก เว็บแอป (Web App) -> ตั้งค่าสิทธิ์เป็น "ทุกคน"🔑 ข้อมูลเข้าระบบเริ่มต้น (Default Login)Username: adminPIN: 123456(หลังจากล็อกอินครั้งแรก แอดมินสามารถเข้าไปแก้ไขชื่อหน่วยงาน หรือรีเซ็ต PIN ได้ที่เมนูจัดการบุคลากร)